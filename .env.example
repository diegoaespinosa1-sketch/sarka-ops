# Stripe — never commit real keys. Set these in Netlify → Site configuration → Environment variables.
# Dashboard: https://dashboard.stripe.com/apikeys

STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# Optional for local Netlify Dev (Netlify also sets URL automatically on deploy)
# DOMAIN=http://localhost:8888
